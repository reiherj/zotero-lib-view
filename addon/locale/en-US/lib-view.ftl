# View menu item, which needs a visible label.
lib-view-toggle-menuitem =
    .label = Grid View

# Toolbar button, which is icon only. A label here gets clipped against the
# search box, so the text lives in the tooltip instead.
lib-view-toggle-button =
    .tooltiptext = Toggle grid view
